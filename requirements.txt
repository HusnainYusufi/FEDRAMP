# Core framework
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.12.5
pydantic-settings==2.13.0

# AWS SDK — read-only access via AssumeRole
boto3==1.34.34
botocore==1.34.34

# Database — PostgreSQL (psycopg3 supports sync + async)
sqlalchemy[asyncio]==2.0.46
psycopg[binary]==3.3.2
aiosqlite==0.20.0
pgvector==0.4.1

# Migrations
alembic==1.18.4

# Logging / observability
structlog==24.1.0

# AI / LangGraph — narrative generation engine
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-core>=0.3.0

# Data processing — FedRAMP controls spreadsheet import
pandas>=2.2.0
openpyxl>=3.1.2

# Templating — FedRAMP AI Studio demo UI
jinja2>=3.1.2

# Markdown rendering (demo UI)
markdown>=3.6

# FastAPI HTML form parsing (required for multipart form handling)
python-multipart>=0.0.9

# Template parsing — DOCX to control blueprints
llama-index-core==0.14.14
llama-index-readers-file==0.5.6
docx2txt==0.9
# PDF parsing (RAG ingestion) — must satisfy llama-index-readers-file>=6.1.3
pypdf>=6.1.3
# LlamaExtract (schema-based extraction)
llama-cloud>=1.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.4
httpx==0.27.0
moto[iam,ec2,s3,rds]==5.0.1
